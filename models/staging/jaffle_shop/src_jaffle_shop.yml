version: 2

sources:
  - name: jaffle_shop
    description: a clone of a Postgres application database
    schema: jaffle_shop
    tables:
      - name: customers
        description: raw customers data
        identifier: raw_jaffle_shop_customers
        columns:
          - name: id #has to be original name of the column in the source not the renamed in stg
            description: primary key of customers data
      - name: orders
        description: raw orders data
        identifier: raw_jaffle_shop_orders
        #freshness: 
          #error_after:
            #count: 2
            #period: day
          #warn_after:
            #count: 1
            #period: day
        #loaded_at_field: "CAST(order_date AS TIMESTAMP)"
  - name: stripe
    schema: stripe
    tables:
      - name: payments
        identifier: raw_stripe_payments  
